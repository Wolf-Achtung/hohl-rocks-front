{
  "name": "hohl.rocks-front",
  "version": "2.1.0",
  "description": "Static site for hohl.rocks (Netlify). API calls are proxied to the Railway backend via netlify.toml.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "serve": "npx serve public",
    "lint": "npx htmlhint 'public/**/*.html' || true"
  }
}
