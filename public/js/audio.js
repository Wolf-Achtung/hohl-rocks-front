if (!window.AudioController){ const audio = new Audio(); audio.loop = true; audio.preload = 'none'; let on = false; window.AudioController = { toggle(){ on = !on; try{ if(on){ audio.play().catch(()=>{}); } else { audio.pause(); } }catch{} return on; } }; }
